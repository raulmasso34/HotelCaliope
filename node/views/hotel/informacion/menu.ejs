<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Menú del Hotel</title>
    <!-- Enlace a Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

    <div class="container mt-5">
        <div class="card shadow-lg p-4">
            <h2 class="text-center mb-4">Menú del Hotel</h2>
            <p class="text-center">ID del Hotel: <strong><?= htmlspecialchars($_GET['id'] ?? '') ?></strong></p>

            <div class="d-flex justify-content-center gap-3">
                <!-- Botón para ver estadísticas -->
                <a href="/estadisticas-hotel/<?= htmlspecialchars($_GET['id'] ?? '') ?>" class="btn btn-primary btn-lg">
                    📊 Ver Estadísticas
                </a>

                <!-- Botón para ver estados -->
                <a href="/estados-hotel/<?= htmlspecialchars($_GET['id'] ?? '') ?>" class="btn btn-secondary btn-lg">
                    📋 Ver Estados
                </a>
            </div>
        </div>
    </div>

    <!-- Script de Bootstrap (JS opcional) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
