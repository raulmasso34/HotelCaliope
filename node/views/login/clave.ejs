<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verificación de Palabra Clave</title>
    
    <link rel="stylesheet" href="/static/css/clave.css">
</head>
<body>
    <div class="verify-container">
        <img src="/logo.png" alt="Logo del Hotel" class="logo">
        <h2>Verificación de Palabra Clave</h2>
        <form action="/verificar-clave" method="POST">
            <input type="password" name="clave" placeholder="Introduce la palabra clave" required>
            <button type="submit">Verificar</button>
        </form>
        <% if (error) { %>
            <p class="error"><%= error %></p>
        <% } %>
    </div>
</body>
</html>
